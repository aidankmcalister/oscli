# oscli docs

This directory contains the Fumadocs site for `oscli`.

Use these commands to work on the docs site locally.

```bash
cd docs
bun install
bun run dev
```

The docs content lives in `docs/content/docs`. Update those MDX files when the
runtime, builder API, or output primitives change.
